# Shared Calendar

## Start application

💡 To start, configure your SMTP server in application.yml, run mvn spring-boot:run, and the scheduler will automatically check every minute for events to notify.

REST endpoints are available at http://localhost:8080/api/events.